import React, { Fragment} from "react";

import Main from "./Main";
import Popup from "./Popup";

const Quiz = (props) => {

  return (
    <Fragment>
      <Main />
      <Popup />
    </Fragment>
  );
};

export default Quiz;
